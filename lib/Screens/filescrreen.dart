import 'package:flutter/material.dart';

class Reuseable_file extends StatefulWidget {
  const Reuseable_file({Key? key}) : super(key: key);

  @override
  State<Reuseable_file> createState() => _Reuseable_fileState();
}

class _Reuseable_fileState extends State<Reuseable_file> {
  @override
  Widget build(BuildContext context) {
    return FutureBuilder(builder: (context ,snapshot){
      return ListTile(

      );
    });
  }
}
